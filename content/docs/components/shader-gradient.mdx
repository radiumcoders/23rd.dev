---
title: Shader Gradient
description: A quiet WebGL color wash for landing heroes, empty states, and marketing sections — sits behind UI, follows the theme, optional pointer follow.
---

import { ShaderGradientDemo } from "@/registry/shader-gradient/shader-gradient-demo"

Not a standalone gradient. Use it as atmosphere behind a hero, wait screen, or empty state so the page feels finished without competing with the copy. Soft sky / sage / cream / lavender on paper in light mode; deep dusk hues in dark. Press `d` to toggle.

<ShaderGradientDemo />

## Installation

<CliCommand command="shadcn@latest add @23rd/shader-gradient" />

Or install straight from the public GitHub source registry:

<CliCommand
  npm="npx shadcn@latest add radiumcoders/23rd.dev/shader-gradient"
  pnpm="pnpm dlx shadcn@latest add radiumcoders/23rd.dev/shader-gradient"
  yarn="yarn dlx shadcn@latest add radiumcoders/23rd.dev/shader-gradient"
  bun="bunx --bun shadcn@latest add radiumcoders/23rd.dev/shader-gradient"
/>

## Usage

Pin it behind a relatively positioned section. Keep content in a later stacking context (`relative z-10`). Default `theme="auto"` follows shadcn / next-themes (`class` on `<html>`).

```tsx
"use client"

import { ShaderGradient } from "@/components/ui/shader-gradient"

export function Hero() {
  return (
    <section className="relative isolate min-h-svh overflow-hidden bg-background">
      <ShaderGradient />
      <div className="relative z-10 mx-auto flex min-h-svh max-w-xl flex-col items-center justify-center px-6 text-center">
        <p className="text-xs font-medium tracking-[0.18em] text-muted-foreground uppercase">
          New
        </p>
        <h1 className="mt-3 text-4xl font-medium tracking-tight">
          Ship the page, not the backdrop
        </h1>
        <p className="mt-3 text-muted-foreground">
          Headline, proof, and a primary action sit on the wash. The shader
          stays quiet.
        </p>
      </div>
    </section>
  )
}
```

Force a palette if the parent theme should not drive it:

```tsx
<ShaderGradient theme="light" />
<ShaderGradient theme="dark" />
```

## Props

| Prop | Type | Default |
| --- | --- | --- |
| `colors` | `string[]` | sky / sage / cream / lavender |
| `speed` | `number` | `0.14` |
| `blur` | `number` | `0.7` |
| `intensity` | `number` | `0.95` |
| `interactive` | `boolean` | `true` |
| `theme` | `"light" \| "dark" \| "auto"` | `auto` |
| `className` | `string` | — |
