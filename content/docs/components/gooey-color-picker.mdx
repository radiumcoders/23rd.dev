---
title: Gooey Color Picker
description: A floating color picker that expands from a swatch into hue and alpha blobs joined by an SVG gooey filter.
---

import { GooeyColorPicker } from "@/registry/gooey-color-picker/gooey-color-picker"

A compact swatch that opens into a panel — circular color wheel, alpha slider, and editable hex field. The same button becomes the close control when open.

<ComponentPreview title="Gooey Color Picker" stageClassName="min-h-[22rem] items-end overflow-visible pb-8 sm:min-h-[26rem]">
  <GooeyColorPicker defaultValue="hsla(320 90% 58% / 1)" />
</ComponentPreview>

## Installation

<CliCommand item="gooey-color-picker" />

Or install straight from the public GitHub source registry:

<CliCommand item="gooey-color-picker" github />

## Usage

<FrameworkCode>
  <FrameworkReact>
```tsx
import { GooeyColorPicker } from "@/components/ui/gooey-color-picker"

export function Example() {
  return (
    <GooeyColorPicker
      defaultValue={{ h: 210, s: 90, l: 55, a: 1 }}
      onChange={(color, css) => {
        console.log(color, css)
      }}
    />
  )
}
```
  </FrameworkReact>
  <FrameworkSvelte>
```svelte
<script>
  import GooeyColorPicker from "$lib/components/ui/gooey-color-picker.svelte"
</script>

<GooeyColorPicker
  defaultValue={{ h: 210, s: 90, l: 55, a: 1 }}
  onChange={(color, css) => {
    console.log(color, css)
  }}
/>
```
  </FrameworkSvelte>
</FrameworkCode>

## Props

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `GooeyColor \| string` | — |
| `defaultValue` | `GooeyColor \| string` | `{ h: 210, s: 90, l: 55, a: 1 }` |
| `onChange` | `(color: GooeyColor, css: string) => void` | — |
| `label` | `string` | `"Color picker"` |
| `className` | `string` | React only |
| `class` | `string` | Svelte only |

`GooeyColor` is `{ h, s, l, a }` (hue 0–360, saturation/lightness 0–100, alpha 0–1). String values accept hex or `hsl`/`hsla`.
